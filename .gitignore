# Dependances
/node_modules

# Build Next
/.next
/out

# Secrets : la chaine de connexion Postgres vit ici et ne doit jamais partir
# sur GitHub. Le depot est en ligne — un mot de passe pousse une fois reste
# dans l'historique meme apres suppression du fichier.
.env
.env*.local

# Divers
.DS_Store
*.log
/.claude

# Cache de compilation incrementale de TypeScript : regenere a chaque build,
# il polluait tous les diffs.
tsconfig.tsbuildinfo
